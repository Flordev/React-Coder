import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/React-Coder/'  // Cambia esto por el nombre correcto de tu repositorio
})
